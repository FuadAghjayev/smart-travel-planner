import 'package:flutter/material.dart';

class ItineraryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Itinerary')),
      body: const Center(
        child: Text('Planned trip itinerary will be here'),
      ),
    );
  }
}